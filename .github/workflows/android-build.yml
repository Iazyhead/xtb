name: Android Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Meson and Ninja
        run: pip install meson ninja

      - name: Download Android NDK
        run: |
          wget https://dl.google.com/android/repository/android-ndk-r27c-linux.zip
          unzip android-ndk-r27c-linux.zip
          echo "ANDROID_NDK_HOME=$PWD/android-ndk-r27c" >> $GITHUB_ENV

      - name: Download Flang Toolchain
        run: |
          wget -O toolchain.tar.bz2 https://github.com/termux/ndk-toolchain-clang-with-flang/releases/download/r29-1/package-install.tar.bz2
          tar -xjf toolchain.tar.bz2
          echo "FLANG_TOOLCHAIN=$PWD" >> $GITHUB_ENV
          echo "--- Checking toolchain contents ---"
          ls -la $PWD/bin/

      - name: Create Meson cross-file
        run: |
          cat > android-arm64.ini << EOF
          [binaries]
          c = '${{ env.FLANG_TOOLCHAIN }}/bin/aarch64-linux-android-clang'
          fortran = '${{ env.FLANG_TOOLCHAIN }}/bin/aarch64-linux-android-flang'
          ar = '${{ env.FLANG_TOOLCHAIN }}/bin/llvm-ar'
          strip = '${{ env.FLANG_TOOLCHAIN }}/bin/llvm-strip'

          [host_machine]
          system = 'android'
          cpu_family = 'aarch64'
          cpu = 'aarch64'
          endian = 'little'

          [properties]
          needs_exe_wrapper = true

          [built-in options]
          default_library = 'shared'
          EOF

      - name: Configure build
        run: meson setup builddir --cross-file android-arm64.ini

      - name: Build project
        run: meson compile -C builddir

      - name: Upload libxtb.so
        uses: actions/upload-artifact@v4
        with:
          name: libxtb-android-arm64
          path: builddir/src/libxtb.so
