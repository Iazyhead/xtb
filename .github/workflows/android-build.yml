name: Android Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Meson, Ninja, and System libc++
        run: |
          pip install meson ninja
          sudo apt-get update
          sudo apt-get install -y libc++-dev libc++abi-dev || true

      - name: Download Android NDK
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r27c-linux.zip
          unzip -q android-ndk-r27c-linux.zip
          echo "ANDROID_NDK_HOME=$PWD/android-ndk-r27c" >> $GITHUB_ENV

      - name: Download Flang and LLVM
        run: |
          # Flang compiler (host package)
          wget -q -O flang-host.tar.bz2 https://github.com/termux/ndk-toolchain-clang-with-flang/releases/download/r29-1/package-flang-host.tar.bz2
          mkdir -p flang-host
          tar -xjf flang-host.tar.bz2 -C flang-host

          # LLVM shared libraries
          wget -q -O full-install.tar.bz2 https://github.com/termux/ndk-toolchain-clang-with-flang/releases/download/r29-1/package-install.tar.bz2
          mkdir -p full-install
          tar -xjf full-install.tar.bz2 -C full-install

          FLANG_BIN=$(find $PWD/flang-host -type f -name 'flang-*' -executable | head -n 1)
          FLANG_LIB_DIR=$(dirname $(dirname "$FLANG_BIN"))/lib

          LLVM_LIB=$(find $PWD/full-install -name 'libLLVM-21.so' | head -n 1)
          LLVM_LIB_DIR=$(dirname "$LLVM_LIB")

          CLANG_DEV_LIB="$PWD/full-install/out/install/linux-x86/clang-dev/lib"

          echo "FLANG_BIN=$FLANG_BIN" >> $GITHUB_ENV
          echo "FLANG_LIB_DIR=$FLANG_LIB_DIR" >> $GITHUB_ENV
          echo "LLVM_LIB_DIR=$LLVM_LIB_DIR" >> $GITHUB_ENV
          echo "CLANG_DEV_LIB=$CLANG_DEV_LIB" >> $GITHUB_ENV

          echo "=== Searching for libc++ everywhere ==="
          find $PWD -name 'libc++*' 2>/dev/null | head -n 20
          find /usr -name 'libc++.so*' 2>/dev/null | head -n 10
          find /usr -name 'libc++abi.so*' 2>/dev/null | head -n 10

          echo ""
          echo "=== Contents of clang-dev lib (filtered for c++) ==="
          ls -la "$CLANG_DEV_LIB" 2>/dev/null | grep -i 'c++' || echo "(none)"

          echo ""
          echo "=== Testing flang with broad LD_LIBRARY_PATH ==="
          LD_LIBRARY_PATH="$CLANG_DEV_LIB:$LLVM_LIB_DIR:$FLANG_LIB_DIR:/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH" \
            "$FLANG_BIN" --version 2>&1 | head -n 5

      - name: Create Flang wrapper
        run: |
          NDK_TOOLCHAIN="${{ env.ANDROID_NDK_HOME }}/toolchains/llvm/prebuilt/linux-x86_64"
          SYSROOT="$NDK_TOOLCHAIN/sysroot"

          cat > flang-wrapper.sh << EOF
          #!/bin/bash
          export LD_LIBRARY_PATH="${{ env.CLANG_DEV_LIB }}:${{ env.LLVM_LIB_DIR }}:${{ env.FLANG_LIB_DIR }}:/usr/lib/x86_64-linux-gnu:\$LD_LIBRARY_PATH"
          exec "${{ env.FLANG_BIN }}" \\
              --target=aarch64-linux-android21 \\
              --sysroot="$SYSROOT" \\
              -resource-dir="$NDK_TOOLCHAIN/lib/clang/18" \\
              -fuse-ld=lld \\
              "\$@"
          EOF
          chmod +x flang-wrapper.sh
          echo "FLANG_WRAPPER=$PWD/flang-wrapper.sh" >> $GITHUB_ENV

          echo "=== Testing wrapper ==="
          ./flang-wrapper.sh --version 2>&1 | head -n 5

          echo ""
          echo "=== Compiling a trivial Fortran file ==="
          cat > /tmp/test.f90 << 'FORT'
          program hello
            print *, "hello from fortran"
          end program hello
          FORT
          ./flang-wrapper.sh -c /tmp/test.f90 -o /tmp/test.o 2>&1 | head -n 20
          ls -la /tmp/test.o 2>&1

      - name: Create Meson cross-file
        run: |
          NDK_BIN="${{ env.ANDROID_NDK_HOME }}/toolchains/llvm/prebuilt/linux-x86_64/bin"

          cat > android-arm64.ini << EOF
          [binaries]
          c = '${NDK_BIN}/aarch64-linux-android21-clang'
          cpp = '${NDK_BIN}/aarch64-linux-android21-clang++'
          fortran = '${{ env.FLANG_WRAPPER }}'
          ar = '${NDK_BIN}/llvm-ar'
          strip = '${NDK_BIN}/llvm-strip'

          [host_machine]
          system = 'android'
          cpu_family = 'aarch64'
          cpu = 'aarch64'
          endian = 'little'

          [properties]
          needs_exe_wrapper = true

          [built-in options]
          default_library = 'shared'
          EOF

      - name: Configure build
        run: |
          meson setup builddir \
              --cross-file android-arm64.ini \
              -Dlapack=custom \
              -Dcustom_libraries= \
              -Dopenmp=false \
              -Dtible=disabled \
              -Dcpcmx=disabled

      - name: Build project
        run: meson compile -C builddir

      - name: Find built libraries
        run: find builddir -name '*.so'

      - name: Upload libxtb.so
        uses: actions/upload-artifact@v4
        with:
          name: libxtb-android-arm64
          path: builddir/**/*.so
